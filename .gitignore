# build output
dist
.vercel

# generated types
.astro/

# dependencies
node_modules

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables — every .env variant except the committed template.
# `vercel env pull` writes .env.local, which carries live credentials.
.env*
!.env.example

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
